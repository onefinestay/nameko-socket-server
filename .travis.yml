language: python
python:
  - "2.7"

install:
  - pip install tox

env:
    - TOX_ENV=test

script:
  - tox -e $TOX_ENV

notifications:
  email: false
  irc:
    channels:
      - "chat.freenode.net#nameko"
    use_notice: true
    skip_join: true
